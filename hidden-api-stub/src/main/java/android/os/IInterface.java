package android.os;

/** compileOnly stub of the framework type (no android.jar on a java-library classpath).
 *  Never shipped; :app resolves the real android.os.IInterface on the bootclasspath. */
public interface IInterface {
    IBinder asBinder();
}
