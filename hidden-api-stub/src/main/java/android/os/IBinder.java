package android.os;

/** compileOnly stub of the framework type (no android.jar on a java-library classpath).
 *  Never shipped; :app resolves the real android.os.IBinder on the bootclasspath. */
public interface IBinder {
}
