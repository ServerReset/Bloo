package android.os;

/** compileOnly stub of the framework type (no android.jar on a java-library classpath).
 *  Never shipped; :app resolves the real android.os.Binder on the bootclasspath.
 *  Implements IBinder like the real class so the AIDL Stub base types line up. */
public class Binder implements IBinder {
}
