package android.content;

/** compileOnly stub of the framework type (no android.jar on a java-library classpath).
 *  Never shipped; :app resolves the real android.content.Intent on the bootclasspath.
 *  Only referenced as a parameter type by the IIntentSender/IIntentReceiver stubs. */
public class Intent {
}
