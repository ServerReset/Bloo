package com.bloo.bluelink.ui

// Liquid glass was removed — it didn't render well and the underlying library
// path was unstable. Floating chrome and cards use the app's standard frosted
// look (frostedRim / glassContainerAlpha in GlassChrome.kt). This file is left
// intentionally empty; there is no glass API to call anymore.
