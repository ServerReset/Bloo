name: Build Android APK

on:
  push:
    branches: [ "**" ]
    tags: [ "v*" ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Skipped on a version-tag push: the `release` job below already does a full
  # build+publish for that case, on the exact same commit. Before this guard,
  # tagging "vN" fired BOTH jobs -- two complete assembleRelease runs (Android
  # SDK setup, full Gradle build, R8) back to back on identical source, plus a
  # confusing rolling "build-N" prerelease published side-by-side with the
  # real "vN" release nobody asked for. Ordinary branch pushes and PRs are
  # unaffected; only a tag push skips this job now.
  build:
    if: "!startsWith(github.ref, 'refs/tags/v')"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Full history + tags, not the default single-commit shallow clone --
          # the changelog step below needs to walk from the previous "build-N"
          # tag to HEAD, and diff/list tags at all.
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # One Gradle invocation, not two: every separate `./gradlew` call repeats
      # the whole project's CONFIGURATION phase (evaluating every build.gradle.kts
      # in :app/:wear/:shared/:uicommon/:hidden-api-stub) before it schedules a
      # single task, regardless of which task was asked for -- so running unit
      # tests and the release build as two invocations paid that cost twice for
      # no benefit. Gradle happily schedules both task graphs from one command
      # line and runs whichever of their shared dependencies only need building
      # once.
      #
      # Still testDebugUnitTest, not testReleaseUnitTest -- the latter isn't a
      # task this project actually has (CI: "Task 'testReleaseUnitTest' not
      # found"), and running it doesn't build or upload a debug APK either way;
      # it only compiles/runs test sources, an internal step with no artifact
      # of its own. Debug build/artifacts are gone from everything below this.
      #
      # assembleRelease is the ONLY thing that runs R8. Both modules shipped
      # `isMinifyEnabled = false` alongside declared proguardFiles for the whole life of
      # the project, so every keep rule in them was dead configuration that read like
      # protection -- and nothing here would have noticed. With shrinking now on, this
      # is what turns "the rules parse and R8 resolves every reference it keeps" into
      # a build-time answer instead of a runtime surprise.
      #
      # It is NOT a proof the release APK works: there is no emulator here, so nothing
      # ever RUNS it. That gap is why renaming stays off (-dontobfuscate) -- see
      # app/proguard-rules.pro. Verifying behaviour still needs a human installing
      # the release actually published below.
      - name: Run unit tests and build release APKs (runs R8 -- the only check on proguard-rules.pro)
        run: ./gradlew testDebugUnitTest assembleRelease --stacktrace

      # assembleRelease also runs lintVitalRelease (AGP does this for release variants
      # only), whose FATAL-severity findings abort the build. Those reports are the one
      # diagnostic this workflow produces that cannot be reconstructed from the run
      # status, and the first thing anyone will want when this step goes red -- so keep
      # them even, especially, on failure.
      - name: Upload lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-reports
          path: |
            app/build/reports/lint-results-*.html
            app/build/reports/lint-results-*.txt
            wear/build/reports/lint-results-*.html
            wear/build/reports/lint-results-*.txt
          if-no-files-found: ignore

      # No actions/upload-artifact for the APKs themselves -- that was reported
      # directly as a real problem, not just an inconvenience: GitHub Actions
      # artifacts are ALWAYS served zipped (a single-file artifact still comes
      # down as a .zip you have to open before you can install it) AND require
      # a signed-in GitHub session to download at all. The GitHub Release
      # published a few steps down (direct, unzipped .apk assets, no auth) is
      # the only APK-delivery path left, and has been the only one the in-app
      # updater (UpdateApi, in :shared) has ever actually read -- confirmed:
      # it parses release.assets[].browser_download_url, never the Actions
      # artifacts API. Removing this saves two uploads (and their zip/upload
      # time) on every single push for a delivery path nothing was using.
      #
      # GitHub's own generate_release_notes only lists merged PRs since the
      # last release -- this repo pushes straight to its branch with no PRs,
      # so that feature was silently producing an empty "## What's Changed"
      # section every single build (just a trailing compare-link line, which
      # UpdateApi.extractChangelog then discarded as not real content). The
      # in-app update tile's "What's new" section was quietly empty as a
      # result. Real per-commit messages since the previous "build-N" tag,
      # written into release_notes.md ourselves, is what UpdateApi actually
      # reads as this build's patch notes.
      - name: Generate changelog
        if: github.event_name == 'push'
        run: |
          git fetch --tags --quiet
          prev_tag=$(git tag -l 'build-*' | sed 's/^build-//' | sort -n | tail -1)
          {
            echo "### How to install"
            echo "1. Download \`Bloo.apk\` below (and \`Bloo-Wear.apk\` for the watch)."
            echo "2. Open the downloaded file from your notification shade or Downloads app."
            echo "3. If Android shows **\"Blocked by Play Protect\"**, tap **\"More details\"**, then **\"Install anyway\"** and confirm with your biometrics or device password."
            echo
            echo "Already have Bloo installed? Just open the app and pull to refresh — it'll offer to download and install this build for you."
            echo
            echo "### What's changed"
            if [ -n "$prev_tag" ]; then
              git log --pretty=format:'- %s' "build-$prev_tag"..HEAD
            else
              git log --pretty=format:'- %s' -n 20
            fi
          } > release_notes.md

      # See the comment above "No actions/upload-artifact for the APKs" for why
      # this Release, not an Actions artifact, is the one and only APK-delivery
      # path. One rolling pre-release per push, tagged by run number so nothing
      # collides with the tag-triggered release job below.
      - name: Publish build as a GitHub Release (direct APK downloads)
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Build #${{ github.run_number }}"
          prerelease: true
          body_path: release_notes.md
          files: |
            app/build/outputs/apk/release/Bloo.apk
            wear/build/outputs/apk/release/Bloo-Wear.apk

  # A separate, manually-tagged Release ("vN") for anyone who wants a stable
  # build instead of the rolling "build-<run number>" pre-release the build
  # job above publishes on every push. Only runs on a version tag push, never
  # on ordinary branch pushes or PRs. The in-app update checker (UpdateApi)
  # ignores these — it only reads "build-" tagged releases, which exist for
  # every ordinary push and carry a comparable run number.
  #
  # No `needs: build` -- the build job above now explicitly SKIPS on a tag
  # push (see its own `if`), specifically so a tag doesn't trigger two full
  # rebuilds of the same commit. A `needs` on a job that never runs on this
  # trigger would make GitHub Actions skip this one too (a skipped dependency
  # doesn't satisfy `needs` by default), silently breaking every tagged
  # release. This job already does its own complete build; it never
  # depended on the other job's outputs anyway.
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # release build type also uses the checked-in debug signing config (see
      # app/build.gradle.kts) so this is a normally-installable signed APK,
      # not an unsigned one — same key as every debug build, so it installs
      # over an existing debug install without an uninstall first.
      - name: Build release APKs
        run: ./gradlew assembleRelease --stacktrace

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app/build/outputs/apk/release/Bloo.apk
            wear/build/outputs/apk/release/Bloo-Wear.apk
          generate_release_notes: true
