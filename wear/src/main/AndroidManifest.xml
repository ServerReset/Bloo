<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_bloo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bloo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlooWear">

        <!-- This watch app requires the phone companion app. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/Theme.BlooWear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Glanceable Wear OS Tile (outside the app): charge + lock at a glance,
             with Lock/Unlock and Open actions. -->
        <service
            android:name=".tile.BlooTileService"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@drawable/ic_bloo"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/ic_bloo" />
        </service>

        <!-- Receives car snapshots + sessions from the phone, and command
             results, even when the watch UI is closed. -->
        <service
            android:name=".WearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/bloo" />
            </intent-filter>
        </service>

        <!-- Watch-face complication: charge % in SHORT_TEXT and RANGED_VALUE slots. -->
        <service
            android:name=".complication.ChargeComplication"
            android:exported="true"
            android:label="Bloo charge"
            android:icon="@drawable/ic_bloo"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.watchface.complications.datasource.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
            <meta-data
                android:name="android.support.wearable.complications.PROVIDER_CONFIG_ACTION"
                android:value="com.bloo.wear.COMPLICATION_CONFIG" />
        </service>

        <!-- Watch-face complication: lock state, tap to lock/unlock. -->
        <service
            android:name=".complication.LockComplication"
            android:exported="true"
            android:label="Bloo lock"
            android:icon="@drawable/ic_shortcut_lock"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.watchface.complications.datasource.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,MONOCHROMATIC_IMAGE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
            <meta-data
                android:name="android.support.wearable.complications.PROVIDER_CONFIG_ACTION"
                android:value="com.bloo.wear.COMPLICATION_CONFIG" />
        </service>

        <!-- Watch-face complication: climate state, tap to start/stop. -->
        <service
            android:name=".complication.ClimateComplication"
            android:exported="true"
            android:label="Bloo climate"
            android:icon="@drawable/ic_shortcut_climate"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.watchface.complications.datasource.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,MONOCHROMATIC_IMAGE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
            <meta-data
                android:name="android.support.wearable.complications.PROVIDER_CONFIG_ACTION"
                android:value="com.bloo.wear.COMPLICATION_CONFIG" />
        </service>

        <!-- Runs a complication tap (lock / climate toggle). -->
        <receiver
            android:name=".complication.ComplicationTapReceiver"
            android:exported="false" />

        <!-- Per-instance car picker, launched by the watch-face complication picker. -->
        <activity
            android:name=".complication.ComplicationConfigActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BlooWear">
            <intent-filter>
                <action android:name="com.bloo.wear.COMPLICATION_CONFIG" />
                <category android:name="android.support.wearable.complications.category.PROVIDER_CONFIG" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
